using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WeaponControllerDrop : MonoBehaviour
{ //nao usado ainda (drop da arma corrente se encontra no pickup, quando eh feita a troca da arma da mao com a do chao)

    // Use this for initialization
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
